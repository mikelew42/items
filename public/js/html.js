;(function(){
var is = utils.is, 
	copy = utils.copy,
	mod = utils.mod,
	sfn = utils.sfn;

HTML = Coll.clone({
	init: function(){
		this.classes = Coll.clone();
		this.attr = Coll.clone();
	}
});

})();